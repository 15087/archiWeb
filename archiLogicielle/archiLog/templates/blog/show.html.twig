{% extends 'base.html.twig' %}

{% block body %}
    <article>
        <h2> {{article.title}}</h2>
        <div class="content">
            <img src="{{article.image}}" style="auto" alt="">
            <p> {{article.content}}</p>
        </div>
    </article>
{% endblock %}